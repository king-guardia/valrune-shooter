# Schemas

JSON Schema definitions for everything in `data/`. Validated in CI — unknown ids,
missing references, and out-of-range values fail the build.

Written in Phase 2a: ability, status, attunement, drone, field object, constants.
